<h1>My Movie list</h1>
<div class="header">
</div>
<div class="container">
  <div class="cards">
      <% @lists.each do |list|%>
    <div class="card-category" >
         <%= link_to list.name, list_path(list)%>
    </div>
      <% end %>
  </div>
  <%= link_to "Create a list", new_list_path %>
</div>
